############################################
# GoldTrader – Cloud environment (template) #
############################################

# Supabase (Cloud) – uzupełnij własnymi danymi z Supabase → Project Settings → API
PUBLIC_SUPABASE_URL=https://<project_ref>.supabase.co
PUBLIC_SUPABASE_ANON_KEY=<public_anon_key>
SUPABASE_URL=https://<project_ref>.supabase.co
SUPABASE_ANON_KEY=<public_anon_key>
# Service Role Key – używany przez cron jobs (NIE udostępniaj w przeglądarce!)
SUPABASE_SERVICE_ROLE_KEY=<service_role_key>

# CI – Supabase Cloud migrations (GitHub Secrets)
# Te wartości ustaw w GitHub → Settings → Secrets → Actions
SUPABASE_PROJECT_REF=<project_ref>
SUPABASE_ACCESS_TOKEN=<supabase_personal_access_token>
SUPABASE_DB_PASSWORD=<database_password_from_Dashboard>

# App
APP_URL=http://localhost:4321

# Provider danych rynkowych (opcjonalne)
# Jeśli METALS_API_KEY jest ustawiony w Vercel ENV, endpoint /api/prices spróbuje użyć Metals.dev API
# do ustalenia bieżącej ceny XAUUSD i na tej podstawie wygeneruje szereg OHLC.
METALS_API_KEY=<metals_dev_api_key>
METALS_API_URL=https://api.metals.dev/v1/latest
METALS_CACHE_TTL_MS=43200000 # cache w ms (domyślnie 12h; ogranicza liczbę zapytań do Metals.dev)

# OpenAI – rekomendacje w panelu newsów (opcjonalne)
# Ustaw w Vercel ENV lub w innym systemie deploymentu
OPENAI_API_KEY=<openai_api_key>
OPENAI_MODEL=gpt-4o-mini
OPENAI_MAX_TOKENS_PER_CALL=300
RECOMMENDATION_CACHE_TTL_MS=600000   # 10 minut cache dla rekomendacji (w ms)
NEWS_EVENTS_CACHE_TTL_MS=300000      # 5 minut cache dla listy wydarzeń (w ms)

# Test accounts (E2E)
E2E_USER_EMAIL=<user@mail.com>
E2E_USER_PASS=<user-pass>
E2E_ADMIN_EMAIL=<admin@mail.com>
E2E_ADMIN_PASS=<admin-pass>

# Vercel Cron Jobs – secret do autoryzacji cron endpointów
# Wygeneruj losowy, długi string (np. openssl rand -hex 32)
CRON_SECRET=<random_secure_secret_here>


